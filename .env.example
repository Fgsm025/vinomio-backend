# Copy to `.env` next to docker-compose.yml, then: docker compose up --build
# PostgreSQL (optional; compose has defaults)
# POSTGRES_USER=vinomio
# POSTGRES_PASSWORD=vinomio
# POSTGRES_DB=vinomio

JWT_SECRET=change-me-in-production
# GROQ_API_KEY=
# LEMON_SQUEEZY_SIGNING_SECRET=
# LEMON_SQUEEZY_CUSTOMER_PORTAL_URL=

# Frontend image: add more VITE_* as build.args under `frontend` in docker-compose.yml
